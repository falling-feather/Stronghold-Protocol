{
  "homepage": "https://github.com/falling-feather/Stronghold-Protocol",
  "name": "stronghold-protocol-geo",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d dist",
    "mp-server": "node server/index.js"
  },
  "devDependencies": {
    "gh-pages": "^6.3.0",
    "typescript": "^5.2.2",
    "vite": "^5.2.0"
  },
  "dependencies": {
    "ws": "^8.20.0"
  }
}
