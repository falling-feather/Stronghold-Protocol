# 工科实验室 - 静态版本

这是工科实验室网站的**纯静态版本**，可以直接部署到 GitHub Pages，无需构建过程。

## 🚀 特点

- ✅ **纯 HTML/CSS/JS** - 无需构建工具
- ✅ **CDN 加载库** - 所有依赖通过 CDN 加载
- ✅ **GitHub Pages 友好** - 可直接部署
- ✅ **快速加载** - 无需下载 node_modules
- ✅ **响应式设计** - 完美适配各种设备

## 📁 文件结构

```
static/
├── index.html      # 主页面（包含所有页面）
├── styles.css      # 所有样式
├── app.js          # 应用逻辑和路由
└── README.md       # 本文件
```

## 🌐 部署到 GitHub Pages

### 方法 1：直接部署 static 目录

1. 将 `static` 目录下的所有文件复制到你的 GitHub 仓库的根目录
2. 在 GitHub 仓库设置中启用 GitHub Pages
3. 选择主分支（main/master）作为源
4. 访问 `https://你的用户名.github.io/仓库名`

### 方法 2：使用 gh-pages 分支

1. 创建 `gh-pages` 分支
2. 将 `static` 目录下的文件复制到该分支的根目录
3. 推送到 GitHub
4. GitHub Pages 会自动从 `gh-pages` 分支部署

### 方法 3：使用 GitHub Actions（推荐）

创建 `.github/workflows/deploy.yml`：

```yaml
name: Deploy to GitHub Pages

on:
  push:
    branches: [ main ]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Deploy
        uses: peaceiris/actions-gh-pages@v3
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./static
```

## 📦 使用的 CDN 库

- **Lucide Icons**: 图标库
- **Three.js**: 3D 图形库（用于未来的 3D 可视化）
- **D3.js**: 数据可视化库（用于未来的图表）
- **MathJax**: 数学公式渲染（用于未来的数学公式）

## 🎨 功能

- ✅ 响应式导航栏
- ✅ 首页展示
- ✅ 四个模块页面（数学、物理、化学、算法）
- ✅ 实验卡片展示
- ✅ 平滑的页面切换
- ✅ 动画效果

## 🔧 本地预览

直接在浏览器中打开 `index.html` 即可预览，或使用简单的 HTTP 服务器：

```bash
# Python 3
python -m http.server 8000

# Node.js (需要安装 http-server)
npx http-server -p 8000

# 然后访问 http://localhost:8000
```

## 📝 添加新功能

### 添加新的实验

在 `app.js` 的 `CONFIG.experiments` 中添加新的实验配置。

### 添加新的页面

1. 在 `index.html` 中添加新的 `<section>` 元素
2. 在导航栏中添加新的链接
3. 在 `app.js` 中添加路由处理

### 添加可视化功能

可以使用 CDN 加载的库：
- **Three.js**: 3D 可视化
- **D3.js**: 数据可视化
- **Chart.js**: 图表库
- **MathJax**: 数学公式

## ⚠️ 注意事项

1. **CDN 依赖**: 需要网络连接才能加载 CDN 资源
2. **浏览器兼容性**: 需要现代浏览器支持（ES6+）
3. **Hash 路由**: 使用 Hash 路由（`#page`）而不是 History API，更适合 GitHub Pages

## 🔄 与 React 版本的区别

| 特性 | React 版本 | 静态版本 |
|------|-----------|---------|
| 构建工具 | 需要 Vite/npm | 无需构建 |
| 依赖管理 | npm install | CDN 加载 |
| 部署 | 需要构建 dist | 直接部署 |
| 文件大小 | 较大（包含 React） | 较小（纯 HTML） |
| 开发体验 | 更好（组件化） | 较简单 |
| 性能 | 需要加载 React | 更快加载 |

## 📚 相关文档

- [GitHub Pages 文档](https://docs.github.com/en/pages)
- [Three.js 文档](https://threejs.org/docs/)
- [D3.js 文档](https://d3js.org/)
- [MathJax 文档](https://www.mathjax.org/)

